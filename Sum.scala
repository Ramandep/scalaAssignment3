package com.knoldus.Assignment3
{
   object sum {
   
     def sum(f: (Int, Int) => Int,a: Int, b:Int):Int = f(a,b)
   }
  
def main(args : Array[String]) {
  println(sum(a*a + b*b,5,6)
  println(sum(a*a*a + b*b*b,5,6))
  println(sum(a+b,5,6))
  
   }

 }
}

}
